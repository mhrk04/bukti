# Server-only Gonka Router credential. Never expose this with NEXT_PUBLIC_.
GONKA_API_KEY=sk-replace-me
GONKA_BASE_URL=https://api.gonkarouter.io
GONKA_MODELS=MiniMaxAI/MiniMax-M2.7,deepseek-ai/DeepSeek-V4-Flash-0731
# Server-only trusted-source search credential. Never expose this with NEXT_PUBLIC_.
# When unset, checks still run without live search (scores stay capped without evidence).
TAVILY_API_KEY=tvly-replace-me
NEXT_PUBLIC_BUKTI_PACKAGE_ID=0xreplace_after_testnet_publish
